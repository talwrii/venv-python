#!/bin/bash
set -o errexit
set -o nounset
set -o pipefail


version=$(python3 setup.py --version)
git tag $version --annotate "$1"
git push --tags

rm -rf dist *.egg-info build
python3 setup.py sdist
twine upload dist/*
